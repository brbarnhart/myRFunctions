% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_add_pairwise.R
\name{bb_add_pairwise}
\alias{bb_add_pairwise}
\title{Add emmeans pairwise comparisons to a ggplot}
\usage{
bb_add_pairwise(
  pw,
  data = NULL,
  mapping = NULL,
  group = NULL,
  dodge_width = NULL,
  y.fun = max,
  y.adjust = NULL,
  step = 0.08,
  hide.ns = FALSE,
  tip.length = 0.01,
  label = "p.signif",
  model = NULL,
  ...
)
}
\arguments{
\item{pw}{An \code{emmGrid} of pairwise contrasts (e.g. \code{pairs(emm)}), or a
data frame with \code{contrast}, \code{p.value}, and any by-variable columns.}

\item{data}{Optional data used to compute y-positions and x / group
levels. Defaults to the plot data.}

\item{mapping}{Optional aesthetic mapping used to resolve \code{x}, \code{y}, and
the dodge grouping (\code{fill}, then \code{colour}, then \code{group}). Defaults to
the plot mapping.}

\item{group}{Optional name of the dodged grouping variable (the factor
being compared, e.g. \code{"Stim"}). Inferred from \code{fill} / \code{colour} /
\code{group} aesthetics when omitted.}

\item{dodge_width}{Width of the dodge, matching
\code{position_dodge(width = ...)}. \code{NULL} (default) infers it from existing
layers, else \code{0.8}.}

\item{y.fun}{Function used to compute the bracket baseline from the
plotted \code{y} values in each cell. Default \link{max}.}

\item{y.adjust}{Extra vertical padding added after \code{y.fun}. \code{NULL}
(default) uses 5\% of the data y-range.}

\item{step}{Fraction of the y-range used to stack multiple brackets at
the same x position. Default \code{0.08}.}

\item{hide.ns}{If \code{TRUE}, drop rows with \code{p.signif == "ns"}.}

\item{tip.length}{Passed to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}.}

\item{label}{Column used as the bracket label. Default \code{"p.signif"}.}

\item{model}{Optional fitted model forwarded to \code{\link[=bb_pairwise_labels]{bb_pairwise_labels()}}.}

\item{...}{Additional arguments passed to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}.}
}
\value{
An object that can be added to a ggplot with \code{+}.
}
\description{
Add significance brackets from \code{pairs(emmeans(...))} to an existing ggplot
with \code{+}. Comparisons on the fill/colour grouping are drawn between dodged
bars at each x position; comparisons on the x-axis span the matching
x categories. By-variables in \code{pw} (for example \code{Sex}) are matched to
facets automatically.
}
\details{
\code{dodge_width} must match the plot's \code{position_dodge(width = ...)}. When
omitted, it is inferred from dodge / jitter-dodge layers.

Bracket height is \code{y.fun} of the plotted \code{y} values in each cell (default
\link{max}, so brackets sit above jittered points), plus padding. If the data
have \code{ymin} / \code{ymax} (as from \code{\link[=bb_emm_df]{bb_emm_df()}}), the CI upper bound is used
so brackets clear error bars. Use \code{y.fun = mean} if the plot shows bars
of means without points.
}
\examples{
\dontrun{
emm <- emmeans(mod, ~ Stim | Sex * Diet * Satiety, type = "response")
pw  <- pairs(emm, reverse = TRUE)

ggplot(df, aes(x = interaction(Diet, Satiety), y = Breakpoint, fill = Stim)) +
  geom_bar(
    stat = "summary", fun = mean,
    width = 0.8, position = position_dodge(width = 0.8)
  ) +
  geom_point(position = position_jitterdodge(dodge.width = 0.8)) +
  facet_wrap(~ Sex) +
  bb_add_errorbar(emm) +
  bb_add_pairwise(pw, hide.ns = TRUE)
}
}
\seealso{
\code{\link[=bb_add_errorbar]{bb_add_errorbar()}}, \code{\link[=bb_pairwise_labels]{bb_pairwise_labels()}}, \code{\link[=bbmake_pairwise_plot]{bbmake_pairwise_plot()}}
}
