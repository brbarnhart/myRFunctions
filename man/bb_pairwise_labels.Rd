% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_pairwise_labels.R
\name{bb_pairwise_labels}
\alias{bb_pairwise_labels}
\title{Build pairwise significance labels for plotting}
\usage{
bb_pairwise_labels(
  pw = NULL,
  emm = NULL,
  pw_table = NULL,
  model = NULL,
  y.adjust = 0,
  step = 0.08,
  hide.ns = FALSE
)
}
\arguments{
\item{pw}{An \code{emmGrid} of pairwise contrasts (e.g. \code{pairs(emm)}), or \code{NULL}
if \code{pw_table} is supplied.}

\item{emm}{An \code{emmGrid} (or data frame / result of \code{\link[=bb_emm_df]{bb_emm_df()}}) used to
place significance brackets above the tallest CI in each panel.}

\item{pw_table}{Optional output of \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}}. Built from
\code{pw} when omitted.}

\item{model}{Optional fitted model forwarded to \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}}
for effect sizes (e.g. Cohen's d).}

\item{y.adjust}{Vertical nudge added to each bracket's base y-position.
A scalar is recycled; a vector is applied after stacking order within
each panel (length must match the number of contrasts or the number of
panels when there is one contrast per panel).}

\item{step}{Fraction of the overall y-range used to stack multiple
brackets within the same panel. Default \code{0.08}. Set to \code{0} to keep all
brackets at the same height (plus \code{y.adjust}).}

\item{hide.ns}{If \code{TRUE}, drop rows with \code{p.signif == "ns"}.}
}
\value{
A tibble with columns \code{group1}, \code{group2}, \code{p.value}, \code{p.signif},
\code{y.position}, \code{effect_annotation}, plus by-variables when present.
Attribute \code{by.vars} records faceting columns.
}
\description{
Creates a data frame suitable for \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}} from
pairwise contrasts: \code{group1}, \code{group2}, \code{p.signif}, \code{y.position}, plus any
by-variables needed for faceting. Also attaches an \code{effect_annotation}
column for optional corner labels.
}
\examples{
\dontrun{
emm <- emmeans(mod, ~ Stim | Group)
pw  <- pairs(emm)
sig <- bb_pairwise_labels(pw, emm = emm, model = mod)

df <- bb_emm_df(emm)
ggplot(df, aes(x = Stim, y = y, ymin = ymin, ymax = ymax)) +
  geom_pointrange() +
  facet_wrap(~ Group) +
  ggpubr::stat_pvalue_manual(
    sig, label = "p.signif",
    xmin = "group1", xmax = "group2",
    y.position = "y.position"
  )
}
}
