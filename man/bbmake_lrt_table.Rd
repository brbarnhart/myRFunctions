% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbmake_lrt_table.R
\name{bbmake_lrt_table}
\alias{bbmake_lrt_table}
\title{Type II likelihood-ratio table}
\usage{
bbmake_lrt_table(model, digits = NULL)
}
\arguments{
\item{model}{A fitted model with a \code{drop1()} method. Typical classes:
\code{glmmTMB}, \code{glm}, \code{lm}, \code{lme4::merMod}. Must include the highest-order
interaction you want tested.}

\item{digits}{Optional number of decimal places for \code{LRT} and \code{p}. \code{NULL}
(the default) leaves full precision so \code{\link[rempsyc:nice_table]{rempsyc::nice_table()}} can
format the paper table. Pass \code{3} to match a hand-rounded pipeline.}
}
\value{
A tibble with columns \code{Term}, \code{Df}, \code{LRT}, and \code{p} (highest-order
terms first). Attribute \code{note} records that the tests are Type II LRTs.
}
\description{
Sequential \code{\link[stats:drop1]{stats::drop1()}} chi-squared tests from the highest-order
interaction down to main effects. This is the LRT analogue of Type II
ANOVA: each term is tested given all other terms of equal or lower order,
respecting marginality.
}
\details{
For \code{glmmTMB} (and other non-Gaussian GLMMs) this is usually preferable to
\code{\link[car:Anova]{car::Anova()}} Wald tests when the sample is small. Wald \emph{p}-values rely on
a quadratic approximation to the log-likelihood that is often poor with
few observations, beta/nbinom families, or few random-effect levels. The
LRT compares actual maximized likelihoods.

Pass the \strong{full} model (the one whose name you would have suffixed with
the highest interaction order, e.g. \code{mod_fc_pref2}). Reduced models are
fitted internally; you do not need to \code{update()} them yourself.

Random-effect terms, offsets, and \code{glmmTMB} zero-inflation / dispersion
formulas are left untouched. Only fixed effects of the conditional model
are tested.
}
\examples{
set.seed(1)
dat <- expand.grid(
  Sex = factor(c("F", "M")),
  Stim = factor(c("A", "B")),
  id = 1:10
)
dat$y <- rpois(nrow(dat), lambda = 4)
mod <- glm(y ~ Sex * Stim, data = dat, family = poisson)
bbmake_lrt_table(mod)

\dontrun{
mod <- glmmTMB::glmmTMB(
  pref ~ Sex * Stim,
  data = dat,
  family = glmmTMB::beta_family()
)
lrt <- bbmake_lrt_table(mod)
rempsyc::nice_table(lrt)
}
}
\seealso{
\code{\link[=bbmake_model_table]{bbmake_model_table()}} for Wald / \emph{F} tables,
\code{\link[stats:drop1]{stats::drop1()}}
}
