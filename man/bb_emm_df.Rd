% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_emm_df.R
\name{bb_emm_df}
\alias{bb_emm_df}
\title{Normalize an emmeans grid into a plot-ready data frame}
\usage{
bb_emm_df(emm)
}
\arguments{
\item{emm}{An \code{emmGrid} from \code{\link[emmeans:emmeans]{emmeans::emmeans()}}, or a data frame already
produced by \code{as.data.frame(emm)} / \code{summary(emm)}.}
}
\value{
A tibble with columns \code{y}, \code{ymin}, \code{ymax}, and the original factor
columns. Attributes:
\describe{
\item{\code{pri.vars}}{Primary (x-axis candidate) factor names}
\item{\code{by.vars}}{By-variable names used for faceting}
\item{\code{mean_col}}{Original mean column name (\code{emmean} or \code{response})}
\item{\code{lower_col}, \code{upper_col}}{Original CI column names}
}
}
\description{
Converts an \code{emmGrid} (or its data-frame summary) into a tibble with
consistent column names for plotting: \code{y} (estimate), \code{ymin} / \code{ymax}
(confidence limits), plus all factor columns from the grid.
}
\examples{
\dontrun{
emm <- emmeans(mod, ~ Stim | Diet * Sex, type = "response")
df  <- bb_emm_df(emm)
ggplot(df, aes(x = Stim, y = y, ymin = ymin, ymax = ymax)) +
  geom_pointrange() +
  facet_grid(Diet ~ Sex)
}
}
