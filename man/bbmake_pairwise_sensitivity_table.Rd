% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbmake_pairwise_sensitivity_table.R
\name{bbmake_pairwise_sensitivity_table}
\alias{bbmake_pairwise_sensitivity_table}
\title{Sensitivity table of planned contrasts across models}
\usage{
bbmake_pairwise_sensitivity_table(
  models,
  specs,
  by = NULL,
  reverse = TRUE,
  type = "response",
  adjust = "none",
  cross.adjust = "holm",
  interaction = NULL
)
}
\arguments{
\item{models}{A \strong{named} list of fitted models (e.g. \code{glmmTMB}, \code{glm},
\code{lm}, \code{lme4::merMod}). Names are used as-is in the \code{Model} column.}

\item{specs}{An emmeans specs formula, passed to \code{\link[emmeans:emmeans]{emmeans::emmeans()}}
(e.g. \code{~ Stim | Diet * Sex}).}

\item{by}{Optional grouping for pairwise contrasts. When \code{NULL} (the
default), the by-variables already encoded in \code{specs} (after \code{|}) are
used. Supply an explicit order such as \code{c("Sex", "Diet")} to control
both pairing and column order.}

\item{reverse}{If \code{TRUE} (the default), reverse the contrast direction
so later factor levels are in the numerator (the usual IRR direction).
Ignored when \code{interaction} is set; pass \code{interaction = "revpairwise"}
for that direction.}

\item{type}{Passed to \code{\link[emmeans:emmeans]{emmeans::emmeans()}}. Default \code{"response"} so count
models return IRRs rather than log-scale differences.}

\item{adjust}{Within-\code{by}-group multiplicity adjustment passed to
\code{\link[emmeans:contrast]{emmeans::contrast()}} (the same argument as \code{pairs(..., adjust = )}).
Default \code{"none"}. Common values: \code{"tukey"}, \code{"bonferroni"}, \code{"holm"},
\code{"fdr"}, \code{"none"}. Adjustment is \strong{within} each \code{by} group; with one
contrast per cell (two-level \code{Stim}) Tukey and none coincide. Ignored
when \code{cross.adjust} is not \code{"none"}.}

\item{cross.adjust}{Adjustment for \strong{every} pairwise test in one model
as a single family (\code{summary(pw, by = NULL, adjust = cross.adjust)}):
all Stim comparisons in all Sex × Diet cells together. Default
\code{"holm"}. Set \code{"none"} to skip it, or
\verb{adjust = "tukey", cross.adjust = "none"} for Tukey within each
by-group. Valid methods are \link[stats:p.adjust.methods]{stats::p.adjust.methods} plus \code{"sidak"}.
Applied separately to each model, not to the stacked rows.}

\item{interaction}{\code{NULL} (the default) runs a simple pairwise contrast.
\code{"pairwise"} or \code{"revpairwise"} is passed to \code{\link[emmeans:contrast]{emmeans::contrast()}}
as \code{interaction}. Factors named in \code{by} are held out; the remaining
factors are contrasted with each other. On \code{~ Stim | Diet * Sex},
\code{by = "Sex"} and \code{interaction = "pairwise"} contrasts Stim and Diet
within Sex. \code{by = NULL} still uses every variable after \code{|}, so that
formula would contrast only Stim and keep Diet as a grouping column.}
}
\value{
A tibble with grouping columns from \code{by} (when present), then
\code{contrast} or the interaction columns (\code{Stim_pairwise},
\code{Diet_pairwise}, and so on), \code{Model}, the test statistic (\code{z.ratio}
or \code{t.ratio}), \code{df} (when present), the effect (\code{IRR}, \verb{Odds Ratio},
or \verb{Mean Difference}), \code{SE}, \code{lower.CL}, \code{upper.CL}, and \code{p.value}.
Rows are ordered by grouping variables, the contrast columns, then
model list order. Values are left at full precision; format them with
\code{\link[=bbnice_pairwise_table]{bbnice_pairwise_table()}}.
}
\description{
Runs the same \code{\link[emmeans:emmeans]{emmeans::emmeans()}} pairwise contrast on each fitted
model in a named list, tidies each with \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}}, and
stacks the results. The list names become the \code{Model} column, so you
can compare a full-data fit against outlier removals (or any other
refits you have already diagnosed).
}
\details{
This is the \strong{pairwise} counterpart to \code{\link[=bbmake_lrt_sensitivity_table]{bbmake_lrt_sensitivity_table()}}.
Pairwise tables ask whether planned cell comparisons change after a
refit; LRT tables ask whether Type II tests of model terms change.

Fit and check diagnostics yourself, then pass the models in:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mods <- list(
  Full = mod_full,
  `Outliers removed` = mod_no_out
)
bbmake_pairwise_sensitivity_table(
  mods, ~ Stim | Diet * Sex, by = c("Sex", "Diet")
)
}\if{html}{\out{</div>}}

On the response scale, pairwise ratios are labelled \code{IRR} (counts,
rates) or \verb{Odds Ratio} (binomial). Gaussian / link-scale pairs use
\verb{Mean Difference}. Random effects are handled by emmeans in the usual
way. The default matches \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}}: Holm across every
comparison \strong{within each model}, not across the stacked rows. A
message reports that count, for example
\verb{Holm across 4 comparisons within each model}.

\code{interaction = "pairwise"} (or \code{"revpairwise"}) contrasts the factors
with each other instead of running a simple pairwise test. Factors
named in \code{by} are held out of that interaction. \code{reverse} does not
change an \code{interaction} value you supply.
}
\examples{
set.seed(1)
dat <- expand.grid(
  Sex = factor(c("F", "M")),
  Stim = factor(c("A", "B")),
  Diet = factor(c("C", "H")),
  id = 1:6
)
dat$y <- rpois(nrow(dat), lambda = 5)
mods <- list(
  Full = glm(y ~ Sex * Stim * Diet, data = dat, family = poisson),
  Reduced = glm(
    y ~ Sex * Stim * Diet,
    data = dat[dat$id != "1", ],
    family = poisson
  )
)
bbmake_pairwise_sensitivity_table(
  mods, ~ Stim | Diet * Sex, by = c("Sex", "Diet")
)
bbmake_pairwise_sensitivity_table(
  mods, ~ Stim | Diet * Sex,
  by = c("Sex", "Diet"),
  cross.adjust = "bonferroni"
)
bbmake_pairwise_sensitivity_table(
  mods,
  ~ Stim | Diet * Sex,
  by = "Sex",
  interaction = "pairwise"
) |>
  bbnice_pairwise_table()
}
\seealso{
\code{\link[=bbnice_pairwise_table]{bbnice_pairwise_table()}} to print this tibble as a flextable,
\code{\link[=bbmake_lrt_sensitivity_table]{bbmake_lrt_sensitivity_table()}} for Type II LRTs across the
same models, \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}} for a single-model pairwise
table, \code{\link[=bbmake_lrt_table]{bbmake_lrt_table()}} for a single-model LRT
}
