% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_add_errorbar.R
\name{bb_add_errorbar}
\alias{bb_add_errorbar}
\title{Add emmeans confidence-interval or SEM error bars to a ggplot}
\usage{
bb_add_errorbar(
  emm,
  interval = c("ci", "sem"),
  sem_mult = 1,
  data = NULL,
  mapping = NULL,
  group = NULL,
  dodge_width = NULL,
  width = 0.2,
  linewidth = 0.6,
  color = "black",
  ...
)
}
\arguments{
\item{emm}{An \code{emmGrid} from \code{\link[emmeans:emmeans]{emmeans::emmeans()}}, or a data frame /
result of \code{\link[=bb_emm_df]{bb_emm_df()}} with \code{y}, \code{ymin}, \code{ymax} (and \code{SE} when
\code{interval = "sem"}).}

\item{interval}{\code{"ci"} (default) uses emmeans confidence limits.
\code{"sem"} uses the estimate ± \code{sem_mult} times the emmeans SE.}

\item{sem_mult}{Multiplier for \code{interval = "sem"}. Default \code{1} (one
standard error). Ignored for \code{"ci"}.}

\item{data}{Optional data used only to copy factor levels onto the
emmeans grid so \code{interaction()} and dodge order match the plot.
Defaults to the plot data.}

\item{mapping}{Optional aesthetic mapping used to resolve \code{x} and the
dodge grouping (\code{fill}, then \code{colour}, then \code{group}). Defaults to the
plot mapping.}

\item{group}{Optional name of the dodged grouping variable (e.g.
\code{"Stim"}). Inferred from \code{fill} / \code{colour} / \code{group} when omitted.}

\item{dodge_width}{Width of the dodge, matching
\code{position_dodge(width = ...)}. \code{NULL} (default) infers it from existing
layers, else \code{0.8}.}

\item{width}{Horizontal width of the T-caps on
\code{\link[ggplot2:geom_errorbar]{ggplot2::geom_errorbar()}}.}

\item{linewidth}{Line width for the error bars.}

\item{color}{Color for the error bars.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_errorbar]{ggplot2::geom_errorbar()}}.}
}
\value{
An object that can be added to a ggplot with \code{+}.
}
\description{
Overlay model-based intervals from \code{\link[emmeans:emmeans]{emmeans::emmeans()}} on an existing
ggplot with \code{+}. This is the interval counterpart to \code{\link[=bb_add_pairwise]{bb_add_pairwise()}}:
dodged \code{fill} / \code{colour} groups get dodged bars, and by-variables (for
example \code{Sex}) are matched to facets automatically.
}
\details{
\strong{Backtransformation.} If you created the grid with
\code{emmeans(..., type = "response")} on a log, logit, or other link-scale
model, \code{interval = "ci"} (the default) uses the confidence limits emmeans
computed on the link scale and then backtransformed. Those intervals are
generally asymmetric on the response scale. That is the same source
\code{\link[=bbmake_pairwise_plot]{bbmake_pairwise_plot()}} uses. \code{interval = "sem"} is the estimate ±
\code{sem_mult * SE} on the plotted scale (delta-method SE after
\code{type = "response"}) and is symmetric; it is not a backtransformed CI.
}
\examples{
\dontrun{
emm <- emmeans(mod, ~ Stim | Sex * Diet * Satiety, type = "response")

ggplot(df, aes(x = interaction(Diet, Satiety), y = Breakpoint, fill = Stim)) +
  geom_bar(
    stat = "summary", fun = mean,
    width = 0.8, position = position_dodge(width = 0.8)
  ) +
  facet_wrap(~ Sex) +
  bb_add_errorbar(emm) +
  bb_add_pairwise(pairs(emm, reverse = TRUE), hide.ns = TRUE)
}
}
\seealso{
\code{\link[=bb_add_pairwise]{bb_add_pairwise()}}, \code{\link[=bb_emm_df]{bb_emm_df()}}, \code{\link[=bbmake_pairwise_plot]{bbmake_pairwise_plot()}}
}
