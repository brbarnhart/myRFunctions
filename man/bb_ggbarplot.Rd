% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_ggbarplot.R
\name{bb_ggbarplot}
\alias{bb_ggbarplot}
\title{Bar plot with mean CI, optional jittered points, and zero line}
\usage{
bb_ggbarplot(
  data,
  x,
  y,
  fill,
  dodge_width = 0.9,
  jitter.width = 0.2,
  point.size = 1.5,
  point.alpha = 0.6,
  point.color = "gray10",
  add_hline = TRUE,
  add_points = TRUE,
  pw = NULL,
  hide.ns = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y, fill}{Character names of columns mapped to x, y, and fill
(same convention as \code{\link[ggpubr:ggbarplot]{ggpubr::ggbarplot()}}).}

\item{dodge_width}{Width passed to \code{\link[ggplot2:position_dodge]{ggplot2::position_dodge()}} and to the
bar width.}

\item{jitter.width}{Horizontal jitter for individual points
(\code{\link[ggplot2:position_jitterdodge]{ggplot2::position_jitterdodge()}}).}

\item{point.size, point.alpha, point.color}{Appearance of individual points.}

\item{add_hline}{If \code{TRUE}, draw a horizontal line at y = 0.}

\item{add_points}{If \code{TRUE}, overlay jittered individual points aligned to
the dodged bars.}

\item{pw}{Optional \code{pairs(emmeans(...))} result (or a pairwise data frame)
added with \code{\link[=bb_add_pairwise]{bb_add_pairwise()}}.}

\item{hide.ns}{Passed to \code{\link[=bb_add_pairwise]{bb_add_pairwise()}} when \code{pw} is supplied.}

\item{...}{Additional arguments passed to \code{\link[ggpubr:ggbarplot]{ggpubr::ggbarplot()}}.}
}
\value{
A ggplot object.
}
\description{
Thin wrapper around \code{\link[ggpubr:ggbarplot]{ggpubr::ggbarplot()}} that applies a consistent theme
and fill palette, optionally overlays individual points (jitter-dodged to
match the bars), and optionally draws a horizontal line at y = 0.
Pass \code{pw} (from \code{pairs(emmeans(...))}) to add significance brackets via
\code{\link[=bb_add_pairwise]{bb_add_pairwise()}}.
}
\examples{
\dontrun{
bb_ggbarplot(df, x = "group", y = "score", fill = "condition") +
  labs(y = "Score")

pw <- emmeans(mod, ~ condition | group) |> pairs(reverse = TRUE)
bb_ggbarplot(df, x = "group", y = "score", fill = "condition", pw = pw)
}
}
\seealso{
\code{\link[=bb_add_pairwise]{bb_add_pairwise()}}
}
