% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbmake_pairwise_plot.R
\name{bbmake_pairwise_plot}
\alias{bbmake_pairwise_plot}
\title{Create a pairwise comparison plot from emmeans}
\usage{
bbmake_pairwise_plot(
  emm,
  x = NULL,
  facets = NULL,
  pw = NULL,
  pw_table = NULL,
  model = NULL,
  reverse = TRUE,
  adjust = "none",
  cross.adjust = "holm",
  connect = TRUE,
  hide.ns = FALSE,
  y.adjust = 0,
  step = 0.08,
  annotate_effect = TRUE,
  y_expand = NULL,
  hjust = 1.05,
  vjust = 1.3,
  point_size = 2,
  linewidth = 0.6,
  errorbar_width = 0.15,
  interval = c("ci", "sem"),
  sem_mult = 1,
  show_zero_line = TRUE,
  color = "black",
  ...
)
}
\arguments{
\item{emm}{An \code{emmGrid} from \code{\link[emmeans:emmeans]{emmeans::emmeans()}}.}

\item{x}{Bare name or string of the x-axis factor. Default: the sole
primary variable on \code{emm} (error if ambiguous).}

\item{facets}{A facet formula (e.g. \code{Diet ~ Sex} or \code{~ Group}), or \code{NULL}
to auto-facet by by-variables (\code{facet_wrap} for one by-var, \code{facet_grid}
for two, \code{facet_wrap} with multiple vars otherwise). Set to \code{FALSE} to
suppress faceting.}

\item{pw}{Optional result of \code{pairs(emm)}. Computed from \code{emm} when both
\code{pw} and \code{pw_table} are \code{NULL}, using \code{reverse}, \code{adjust}, and
\code{cross.adjust}. Pass this only for a custom contrast family.}

\item{pw_table}{Optional output of \code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}}. Built from
\code{pw} when omitted. If you already have a table, p-values are used as-is
(\code{adjust} / \code{cross.adjust} are not re-applied).}

\item{model}{Optional fitted model used for Gaussian effect sizes
(Cohen's d). Recovered from \code{emm} / \code{pw} when possible; not needed for
IRR / odds-ratio plots.}

\item{reverse}{If \code{TRUE} (the default), reverse pairwise direction so
later factor levels are in the numerator. Ignored when \code{pw} or
\code{pw_table} is supplied.}

\item{adjust}{Within-\code{by}-group multiplicity adjustment used when \code{pw}
is computed from \code{emm}. Default \code{"none"}. Ignored when \code{pw} or
\code{pw_table} is supplied, and ignored when \code{cross.adjust} is not \code{"none"}.}

\item{cross.adjust}{Adjustment for \strong{all} pairwise tests on the plot
as one family (every Stim pair in every Sex × Diet cell). Default
\code{"holm"}. Set \code{"none"} for no family-wide correction, or
\verb{adjust = "tukey", cross.adjust = "none"} for Tukey within each
by-group. Ignored when \code{pw_table} is supplied.}

\item{connect}{If \code{TRUE}, draw lines connecting points within each panel
along \code{x} (grouped by by-variables).}

\item{hide.ns}{Passed to \code{\link[=bb_add_pairwise]{bb_add_pairwise()}} and \code{\link[=bb_pairwise_labels]{bb_pairwise_labels()}}.}

\item{y.adjust}{Vertical nudge for significance brackets.}

\item{step}{Stacking step for multiple brackets within a panel (fraction
of y-range). See \code{\link[=bb_add_pairwise]{bb_add_pairwise()}}.}

\item{annotate_effect}{If \code{TRUE}, draw one effect-size label per panel
(first contrast in that panel). When \code{y_expand} is \code{NULL}, this also
uses extra top padding so the annotation is not cramped against the
panel edge.}

\item{y_expand}{Y-axis range expansion passed to
\code{\link[ggplot2:scale_y_continuous]{ggplot2::scale_y_continuous()}}. \code{NULL} (default) uses
\code{expansion(mult = c(0.02, 0.28))} when \code{annotate_effect} is \code{TRUE}
and \code{expansion(mult = c(0.02, 0.18))} otherwise. A numeric of length
1 or 2 is treated as multiplicative padding
(\code{expansion(mult = y_expand)}). Otherwise pass the result of
\code{\link[ggplot2:expansion]{ggplot2::expansion()}}.}

\item{hjust, vjust}{Position adjustments for the effect-size annotation
(placed at \code{x = Inf}, \code{y = Inf}). Text size is not hardcoded: it
follows the plot theme (ggplot2 4.0+: \code{theme_*(base_size)} or
\code{theme(geom = element_geom(fontsize = ...))}).}

\item{point_size}{Size passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} (\code{size}).}

\item{linewidth}{Line width for error bars and connecting lines.}

\item{errorbar_width}{Horizontal width of the T-caps on
\code{\link[ggplot2:geom_errorbar]{ggplot2::geom_errorbar()}}.}

\item{interval}{\code{"ci"} (default) uses emmeans confidence limits (the
backtransformed limits when the grid was created with
\code{type = "response"}). \code{"sem"} uses the estimate ± \code{sem_mult} times
the emmeans SE. See \code{\link[=bb_add_errorbar]{bb_add_errorbar()}}.}

\item{sem_mult}{Multiplier for \code{interval = "sem"}. Default \code{1}.}

\item{show_zero_line}{If \code{TRUE}, draw a horizontal line at y = 0.}

\item{color}{Color for points / lines / error bars.}

\item{...}{Additional arguments reserved for future use (currently unused).}
}
\value{
A ggplot object. Add labels/themes with \code{+} as usual.
}
\description{
High-level convenience wrapper around \code{\link[=bb_emm_df]{bb_emm_df()}} and
\code{\link[=bb_add_pairwise]{bb_add_pairwise()}}. Builds a ggplot with points, T-capped 95\% CI
error bars, optional connecting lines, faceting by by-variables,
significance brackets, and a single effect-size annotation per panel.
}
\details{
P-values default to Holm across every pairwise test
(\code{adjust = "none"}, \code{cross.adjust = "holm"}). The same message as
\code{\link[=bbmake_pairwise_table]{bbmake_pairwise_table()}} reports the count, for example
\verb{Holm across 6 comparisons}. A prebuilt \code{pw_table} is used as-is.

For custom layouts, call the helpers yourself and add intervals /
brackets with \code{\link[=bb_add_errorbar]{bb_add_errorbar()}} and \code{\link[=bb_add_pairwise]{bb_add_pairwise()}}.
}
\examples{
\dontrun{
emm <- emmeans(mod, ~ Stim | Diet * Sex, type = "response")

bbmake_pairwise_plot(emm) +
  labs(x = "Stimulation", y = "Breakpoint (active pokes)")

# Unadjusted p values
bbmake_pairwise_plot(emm, cross.adjust = "none")

# Tukey within each by-group
bbmake_pairwise_plot(emm, adjust = "tukey", cross.adjust = "none")

# Custom pairs still work. The default Holm adjustment is applied
# unless cross.adjust = "none" or pw_table is supplied.
bbmake_pairwise_plot(emm, pw = pairs(emm, reverse = TRUE, adjust = "tukey"))

# Extra top padding (multiplicative c(bottom, top))
bbmake_pairwise_plot(emm, y_expand = c(0.02, 0.4))

# Deeper control with helpers
df <- bb_emm_df(emm)
ggplot(df, aes(x = Stim, y = y, fill = Diet)) +
  geom_point() +
  facet_wrap(~ Sex) +
  bb_add_errorbar(emm) +
  bb_add_pairwise(pairs(emm))
}
}
