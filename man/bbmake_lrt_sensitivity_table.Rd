% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbmake_lrt_sensitivity_table.R
\name{bbmake_lrt_sensitivity_table}
\alias{bbmake_lrt_sensitivity_table}
\title{Sensitivity table of Type II LRTs across models}
\usage{
bbmake_lrt_sensitivity_table(models, digits = NULL)
}
\arguments{
\item{models}{A \strong{named} list of fitted models (e.g. \code{glmmTMB}, \code{glm},
\code{lm}, \code{lme4::merMod}). Names are used as-is in the \code{Model} column.
Each model is passed to \code{\link[=bbmake_lrt_table]{bbmake_lrt_table()}}.}

\item{digits}{Optional number of decimal places for \code{LRT} and \code{p}.
\code{NULL} (the default) leaves full precision so \code{\link[rempsyc:nice_table]{rempsyc::nice_table()}}
can format the paper table.}
}
\value{
A tibble with columns \code{Term}, \code{Model}, \code{Df}, \code{LRT}, and \code{p}.
Rows are ordered by the Type II term order of the first model, then
by model list order. Attribute \code{note} records that the tests are
Type II LRTs stacked across models.
}
\description{
Runs \code{\link[=bbmake_lrt_table]{bbmake_lrt_table()}} on each fitted model in a named list and
stacks the results. The list names become the \code{Model} column, so you
can compare a full-data fit against outlier removals (or any other
refits you have already diagnosed).
}
\details{
This is the \strong{omnibus} counterpart to
\code{\link[=bbmake_pairwise_sensitivity_table]{bbmake_pairwise_sensitivity_table()}}. Pairwise tables ask whether
planned cell comparisons change after a refit; LRT tables ask whether
Type II tests of model terms (main effects and interactions) change.
Both are appropriate and complementary. Likelihood-ratio tests are
used rather than Wald / \emph{F} ANOVA because that is the package's
preferred term test for GLMMs; see \code{\link[=bbmake_lrt_table]{bbmake_lrt_table()}}.

Fit and check diagnostics yourself, then pass the models in:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mods <- list(
  Full = mod_full,
  `Outliers removed` = mod_no_out
)
bbmake_lrt_sensitivity_table(mods)
}\if{html}{\out{</div>}}
}
\examples{
set.seed(1)
dat <- expand.grid(
  Sex = factor(c("F", "M")),
  Stim = factor(c("A", "B")),
  Diet = factor(c("C", "H")),
  id = 1:6
)
dat$y <- rpois(nrow(dat), lambda = 5)
mods <- list(
  Full = glm(y ~ Sex * Stim * Diet, data = dat, family = poisson),
  Reduced = glm(
    y ~ Sex * Stim * Diet,
    data = dat[dat$id != "1", ],
    family = poisson
  )
)
bbmake_lrt_sensitivity_table(mods)
}
\seealso{
\code{\link[=bbmake_pairwise_sensitivity_table]{bbmake_pairwise_sensitivity_table()}} for planned contrasts
across the same models, \code{\link[=bbmake_lrt_table]{bbmake_lrt_table()}} for a single-model LRT,
\code{\link[=bbmake_model_table]{bbmake_model_table()}} for Wald / \emph{F} tables
}
